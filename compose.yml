name: neurocalc-landing

services:
  landing:
    build:
      context: .
      dockerfile: Dockerfile
    image: neurocalc-landing:latest
    container_name: neurocalc-landing
    restart: unless-stopped
    networks:
      - traefik
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=root_default"
      - "traefik.http.routers.landing.rule=Host(`neurocalc.com.br`) || Host(`www.neurocalc.com.br`)"
      - "traefik.http.routers.landing.entrypoints=web,websecure"
      # TLS publico e terminado na Cloudflare (modo Full). O Traefik so precisa
      # aceitar HTTPS na origem com o cert default; sem certresolver p/ nao ficar
      # tentando (e falhando) emitir Let's Encrypt atras do proxy da Cloudflare.
      - "traefik.http.routers.landing.tls=true"
      - "traefik.http.services.landing.loadbalancer.server.port=80"

# Rede externa do Traefik (projeto root/n8n). Nao e criada aqui, apenas referenciada.
networks:
  traefik:
    name: root_default
    external: true
